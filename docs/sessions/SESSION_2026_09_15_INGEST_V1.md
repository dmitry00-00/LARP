# Приём v1: словарь, витрина, реестр сообществ, регламенты, первый парсер — 15.09.2026

> **Триггер:** владелец: «RU и DE/NL одновременно с ориентацией на RU»;
> «§7 SUMMARY бери целиком и делаем по порядку».
>
> **Метод:** сборка `apps/ingest/` поверх `kernel/`, каждый шаг — прогоном с
> числом и знаменателем; страница проверена рендером в Chromium.
>
> **Стенд:** `apps/ingest/.venv` (Python 3.14, свой; kernel — 196 тестов
> проходят в нём), SQLite `data/hmb.sqlite`, страница `site/index.html` из
> `file://`.

---

## 1. Что теперь есть на диске

| что | где | проверено |
|---|---|---|
| приложение приёма: модели (9 таблиц), БД, словарь слотов, HTTP-клиент с паузами, 5 импортёров, классификатор, экспорт фида, CLI | `apps/ingest/hmb/` | `python -m hmb {init,import,classify,export,status}` |
| **словарь слотов под ЛАРП**: 49 слотов × 9 групп, **1 686 алиасов** (en 587 · de 476 · ru 417 · nl 203 · kk 3) | `hmb/seeds/slots.json` (v8) | покрытие на трёх каталогах — §2, §7 |
| **три витрины**: Epic Armoury 1 872 · Calimacil 3 929 · Dein LARP Shop 3 217 = **9 018 товаров**, 5 044 в наличии | `lots` (`kind=catalog`) | 89.2 % / 96.6 % / 88.5 % со слотом |
| **реестр игр и сообществ**: 988 игр kogda-igra, 880 с VK-группой, 34 с TG-каналом | `games` | курсор `sources.meta.cursor_ts` двигается |
| **регламенты**: 5 систем, 39 числовых пределов | `rulesets`, `ruleset_limits` | из `seeds/rulesets.json` |
| парсер объявлений kleinanzeigen.de + классификатор (направление, состояние, дайджест, слот, гейт для лент сообществ) | `importers/kleinanzeigen.py`, `classify.py` | 15 офлайн-тестов; **живые прогоны: 165 объявлений → 161 лот + 4 дайджеста** (§2, §7) |
| обход TG-каналов игр через `t.me/s/` (вне квоты) | `importers/tg_web.py` | 22 канала, 435 постов → **0 лотов** (§7) |
| парсер OLX kz/ua по JSON-контракту | `importers/olx.py` | **не проверен**: 403 с этого egress — §4 |
| живой фид на странице: `meta.synthetic=false`, 5 000 лотов (потолок; объявления первыми) | `site/data/feed.json`, `site/seed.js` | Chromium: 0 ошибок JS; снимок `site-live-2026-09-15.png` (ранняя версия, 1 700 строк) |
| заготовка публикации на GitHub Pages | `.github/workflows/pages.yml`, `.gitignore` | не запускалась: репозитория нет, `git init` — решение владельца |
| синтетический фид 05.09 | `data/feed_synthetic_2026-09-05.{json,js}` | сохранён, не удалён |

Три числа (15.09.2026 17:35 MSK, `hmb status`): **лотов всего 9 179 · со слотом
8 470 (92.3 %) · объявлений (kind=ad) 161** — все DE, все `offer`; русскоязычных
объявлений в базе нет. Ранний замер того же дня: 1 997 / — / 125.

## 2. Замеры

**Покрытие словаря на каталоге Epic Armoury** (1 872 товара): v1 — 80.4 %,
v2 (после 40 алиасов из непокрытых типов + правило «держатель важнее
оружия» + слоты `kit`, `banner`, `props`, `weapon_parts`) — **87.6 %**. Остаток
232: 197 без `product_type` с именами собственными («Dorgen», «Shahin»,
«Odo II») — без коллекций их в класс не разложить; 22 «Foam Weapons»
без родового слова (зелья, бомбы, артефакты); Yoremade-одежда (cotehardie —
добавлен, «shift», «smock», «braies» — нет). Правило дома: **каждый новый
посев — новый замер**, не «кажется, лучше».

**Спецификации из `products.json`**: поля «Length: / Weight:» в JSON
отсутствуют — только проза. Регексом по прозе: длина у 266, сердечник у 285,
покрытие у 273, материалы у 1 596, масса у 10. Структурные поля живут на
HTML-странице товара (1 866 запросов ≈ 50 мин с паузой) — отдельный проход,
если понадобится масса для валидатора.

**Словарь на реальных объявлениях (kleinanzeigen, 125 лотов):** v2 — **29.6 %
без слота**: немецкие множественные числа («Wappenröcke», «Kapuzenumhänge»,
«Krüge»), «Konvolut» (сборный лот), родовые «Rüstung» / «Waffe» без вида,
костюм целиком, мебель и декор лагеря, книги. v3 (+166 алиасов, слоты
`costume`, `furniture_decor`, `other_misc`) — 16.8 %; v4 (+`weapon_other`,
родовые слоты уступают конкретным) — **2.4 %** (3 из 125: два косплей-реквизита
«Predator Cutting Disc», один битый заголовок). ⚠ Это **in-sample**: словарь
досеян по тем же 125 объявлениям; настоящую долю покажет следующая свежая
партия. Состояние: `used` 22 · `new` 5 · `unknown` 98 — большинство
объявлений состояние не называют, и это честный `unknown`, не «плохое».
Направление: 125 `offer`, 0 `want` — на kleinanzeigen спроса не публикуют.

**Дефект правила слота, пойманный замером:** «Breda Rapier Holder» → `sword_1h`
по порядку слов. Исправлено: слоты-контейнеры (`scabbard`, `quiver`, `belt`,
`kit`, `weapon_parts`, `banner`) побеждают упомянутое рядом оружие.

## 3. Ловушки, стоившие прогона

| ловушка | как выглядела | правда |
|---|---|---|
| SQLite — один писатель | `database is locked` у `import rulesets`, пока шёл обход календаря | длинная транзакция без коммитов запирает базу для всех; коммит батчами по 50 + `busy_timeout=60` |
| вторая страница kleinanzeigen | `/s-larp/seite:2/k0` дал «738 Ergebnisse für „seite:2“» | формат `/s-seite:2/larp/k0`; первый прогон (148 строк, ~75 мусора) **стёрт целиком** — проще, чем отличать |
| темп 1.5 с | 403 «IP-Bereich vorübergehend gesperrt» на 14-м запросе | 4 с и остановка **всего** прогона при 403 с отметкой в `sources.meta.blocked_at` (recruit: бюджет спрашивать внутри прогона) |
| OLX «открытый JSON без токена» | 403 CloudFront на весь сайт, любой UA | гео-блок EU-egress (узел CPH); сборщик читал через другой выход. Не обходить; запускать из KZ/UA |
| naive datetime из SQLite | `can't subtract offset-naive and offset-aware` в экспорте | SQLite отдаёт naive даже при `timezone=True`; нормализовать на чтении |
| `packages.find` в kernel | `pip install -e kernel` падал: «Multiple top-level packages: ops, kernel» | `include = ["kernel*"]` в `pyproject.toml` |

## 4. Не закрыто в этой сессии

* **kleinanzeigen — бюджет запросов.** Блок сняли через 5 минут; за один
  прогон проходит ~8 запросов при паузе 4 с, потом снова 403 «IP-Bereich
  vorübergehend gesperrt» — ограничение по числу запросов в окне, а не по
  темпу. Импортировано 129 объявлений (4 запроса из 7). Следствие для
  расписания: **не больше 6–8 страниц за прогон, прогоны раз в час**; полный
  обход 7 запросов × 2 страницы — за два прогона. Полей `posted_at` у
  большинства нет точнее дня («Gestern, 15:17» — относительная дата).
* **OLX kz/ua** — прогон только из Казахстана/Украины.
* **Классификатор** — правила покрыты офлайн-тестами; замер §2 — in-sample.
  Свежая партия kleinanzeigen через час даст первую честную долю `no_slot`.
* **Архетипы под ЛАРП** — в фиде прежние 8 (ИСБ), помечено `meta.archetypesNote`.
  Страница по-прежнему называется «реестр снаряжения для ИСБ» (`site/index.html`).
* **Alembic** — нет, `create_all`. Первая миграция — когда схема переживёт
  первую правку.
* **Реестр сообществ → обход**: 880 VK-групп и 34 TG-канала лежат в `games`,
  но в `sources` не заведены и не обходятся. Следующий шаг — `t.me/s/` по 34
  каналам через `kernel/clients/tg_web.py` (вне квоты).
* **Ярмарка Мастеров, VK, TG-чаты** — за стеной, как и было.

## 5. Странности — владельцу

1. **`posted_at` у витрины** = `published_at` Shopify (дата публикации
   карточки, бывает в будущем — «−1д» в колонке возраста). Для витрины
   возраст бессмыслен; показывать «—» для `kind=catalog`?
2. **Цены витрины в фиде — в ₽ через якорь USD** (статический фолбэк курса, фетч
   не делался). Страница показывает в валюте профиля (₸). Курсы — заглушка
   уровня «порядок величины», как и было помечено.
3. 116 из 1 104 записей `/api/changed` — удалённые игры; их нет в базе. Хранить
   факт удаления или нет?
4. Первый прогон kleinanzeigen стёрт вместе с 73 настоящими объявлениями —
   они вернутся повторным прогоном, но `created_at` у них будет позже. Для
   метрики «отставание обхода» это шум одного дня.

## 6. Решения владельца, зафиксированные сессией

* Ниша — ЛАРП (15.09, записка `NICHE_PIVOT_LARP`).
* **RU и DE/NL одновременно, ориентир — RU**: словарь трёхъязычный с первого
  посева, первый парсер объявлений — DE (kleinanzeigen), RU-объявления ждут
  аккаунта владельца (VK/TG) и egress KZ (OLX).
* «Только вы» про VK/TG = только ваши учётные данные, не ручной труд:
  сессия Telegram и токен VK — ваши, обход — автоматический; секреты в файлы
  агент не пишет.

## 7. Продолжение того же дня («дальше»)

Всё ниже — по правилу «каждый посев — новый замер»; числа сняты 17:35 MSK.

**Свежая партия kleinanzeigen — первая честная доля.** Второй прогон (после
снятия блока, пауза 4 с) дал **36 новых объявлений, 0 без слота** — словарь v8
на out-of-sample партии не промахнулся ни разу. Общий счёт: 165 в `inbox`,
161 лот, 4 дайджеста; без слота 3 из 161 (те же три, что в §2). Состояние:
`used` 23 · `new` 10 · `unknown` 128.

**Ротация запросов.** Бюджет сайта — считанные запросы на окно (первый прогон
8, потом 3 до 403), а прогон всегда начинал с первого запроса и до последних
не доходил никогда. Теперь курсор `sources.meta.next_query_idx`: следующий
прогон начинает с запроса, на котором остановился. Замер окна: прогон 17:28
получил 403 первым же запросом — **блок держится дольше 15 минут**; фоновый
цикл с шагом 15 мин пробует ещё трижды (17:43 · 17:58 · 18:13), результат —
в следующей записке.

**Ещё две витрины (Shopify `/products.json`, тот же импортёр с `site=`).**
Calimacil (CA, CAD) — 3 929 товаров, **96.6 %** со слотом. Dein LARP Shop (DE,
EUR) — 3 217, **88.5 %**. Итого 9 018 карточек трёх магазинов.

**Ловушка-алиас `standard` (найдена на Dein LARP Shop).** Алиас `standard`
слота `banner` (штандарт) зажигался на теге `shipping-standard`, которым
помечены 1 387 карточек, и «покрытие» выглядело как 100 % там, где слот был
`banner` у перчаток. Алиас снят; честное покрытие упало до 71.6 %, после слота
`crafting_supplies` (латекс, пена, краска — группа `supplies`) — 88.5 %. Это
тот же класс, что «ложные алиасы» в recruit: ключ, зажигающийся на служебной
лексике источника, делает из мусора запись со слотом, невидимую фильтру
«без слота». Регрессионный тест: `shipping-standard` → `None`.

**Немецкие композиты.** «Elfenschwert», «Kurzschwert», «Langbogen» — одно
слово, алиас по границам слов не срабатывает. Добавлен второй проход
`SlotMatcher._DE_HEADS`: если словарь молчит, ищем голову композита
(`schwert`, `bogen`, `schild`, `helm`, …) с немецкими окончаниями
множественного числа. Побочный дефект по дороге: `schaumstoff` (пена) как
алиас `weapon_parts` перебивал оружие в «Elfenschwert, Schaumstoff» —
`weapon_parts` выведен из слотов-контейнеров, алиас снят.
Правило приоритета теперь: контейнер (`scabbard`, `quiver`, `jewelry`, `belt`,
`kit`, `banner`) > конкретный слот > родовой (`weapon_other`, `costume`,
`other_misc`); 10 параметризованных случаев в `tests/test_slot_priority.py`.
«Choker Schwert Ketten» → `jewelry`, «Breda Rapier Holder» → `scabbard`.

**Каналы игр — не рынок.** 34 `telegram_channel` из `games` → 30 хендлов
(8 приглашений `+…` пропущены: закрытые чаты, через `t.me/s/` не читаются) →
22 канала с превью → **435 постов → 0 лотов**. Первая версия гейта пропустила
9 ложных лотов на словах «цена», «взнос» (взносы за игру — не продажа
снаряжения); гейт ужесточён: для источников `tg`/`vk`/`calendar` нужен глагол
сделки (`продам`/`куплю`/`отдам`/`verkaufe`/`suche`/`te koop` …) **и** слот в
первых 300 символах, иначе `skipped/off_topic`. Гейт стоит до проверки на
дайджест. Вывод подтверждает SUMMARY §3: каналы игр — анонсы, рынок живёт в
чатах за стеной (учётные данные владельца).

**Экспорт.** Фид отдаёт объявления первыми, затем витрины до потолка 5 000;
подписи слотов — полные `short`; мастер витрины = продавец магазина, а не
«мастер не указан». `feed.json` — 3.3 МБ.

**Первая RU-витрина — Донжон (`importers/donjon.py`, 18:00).** Собственная
HTML-витрина (не Shopify): 55 разделов меню, 94 запроса, **1 770 карточек**
(sitemap обещает 6 896 URL товаров — остальное архив, в листингах не
показывается), 1 283 в наличии; «Барахолка» — 13 б/у-позиций в ₽, записаны
как `kind=ad, condition=used`, город Москва, продавцы кодами как пришли.
Покрытие: по названию **86.6 %**, с подписью раздела 97.6 % — и подпись
врала: «Каролинг тип X» под «Клинковое оружие» уезжал в `weapon_other`,
«Набор бубенчиков» под «Подвески и амулеты» — в `scabbard`.

Три дефекта словаря, найденные на RU-номенклатуре (все — свойство
`kernel/ner_extractor`: голова кириллического ключа закрыта, **хвост открыт**):

1. **Русский алиас должен быть основой.** «фибула» не находит «фибулы»,
   «корона» — «короны»: 12 «фибул парных» без слота при живом алиасе.
   Лечение — `slots.ru_stems()`: для однословных RU-алиасов от 5 букв в
   словарь добавляется основа без последней гласной (1 762 алиаса в базе →
   1 899 ключей у матчера). Покрытие donjon по названию 86.6 → **94.6 %**.
2. **Ловушки префикса**: «венец» → «Венецианский палаш», «набор» → «вакидзаси
   с набором» (гарнитура клинка), «подвес» → «подвески», «шест» → «Шестовицы»,
   «сет» → «сетка», «кит» → «китайский», «нож» → «ножнами». Сняты из посева;
   посев (`seed_slots`) теперь удаляет из базы алиасы `origin=seed`, которых
   нет в файле — раньше снятое из файла жило в базе дальше.
3. **«Меч с ножнами» — меч, а не ножны.** Контейнерное правило («держатель
   важнее оружия», §2) на RU дало 40 мечей и кинжалов в `scabbard`. Фраза
   «с/в [прил.] ножнами | with scabbard | mit Scheide | met schede» вырезается
   до поиска (`_INCLUDED_RX`); «Ножны для меча» без предлога — ножны.

Досеяно из номенклатуры Донжона: каролинг, скрамасакс, сторта, кортелас,
фальката, спата, кацбальгер (мечи); хатангу-дегель, куяк, панцирь,
колонтарь, юшман, бахтерец (корпус); рондели, брассары, армпоинты, сабатоны,
панцершоссы; мисюрка, ерихонка, кабассет, морион; кисет, колты, височные
кольца, застёжки; эфес, древко, гарда, навершие (`weapon_parts`); кольчужные
кольца, клепальник (`crafting_supplies`). Словарь **v9: 49 слотов, 1 762
алиаса** (ru 470).

**`hmb reslot [--source]`** — пересчёт слотов по сохранённым полям после
посева (для объявлений — по `raw_text` сырья, как в classify). Замер v9,
18:05: donjon **99.9 %** (1 768/1 770; без слота «Солдатик „Варяг“» и
«Драккар „Осберг“» — сувениры), kleinanzeigen 98.8 % (159/161, те же два
косплей-диска), Epic Armoury 89.2 → **89.4 %**, Calimacil 96.6 → **96.7 %**,
Dein LARP Shop 88.5 → **91.6 %** (витрины перечитаны импортом — у них слот
считается ещё и по тегам, которых `specs` не хранит). Итого в базе 10 949
лотов, со слотом 94.5 %; фид — 5 000 (174 объявления впереди), 28 тестов.

**kleinanzeigen, бюджет измерен фоновым циклом (шаг 15 мин).** 17:28 — 403
первым же запросом; 17:43 — 4 запроса, **+68 объявлений**; 17:59 — 3 запроса,
0 новых, 403; 18:15 — 6 запросов, 0 новых, 403. Итого: **4–7 запросов на
окно ~15 мин**, блок снимается к следующему шагу; ротация запросов работает
(за 4 прогона обойдены все 7). Второй `classify` (18:28): 68 сырых → **67
лотов + 1 дайджест, без слота 1 (1.5 %)** — вторая out-of-sample партия
подтверждает словарь. Объявлений в базе **241** (228 DE + 13 RU); состояние
`used` 44 · `new` 18 · `unknown` 179.

**Ещё две RU-витрины (18:00–18:20).** WarGear Shop (`importers/wargearshop.py`,
Storeland): 255 категорий из sitemap, 42 пустых (родители), **2 756 карточек**,
наличия в листинге нет — все `active` как честное «неизвестно». По названию
словарь даёт **65.4 %** (названия короткие: «Ноги Пехотные Базовые», «Бэйдж
„Кот“»), с хлебными крошками — **99.3 %**; не закрыты 10 «Защита корпуса»
(куртки HEMA без слова «куртка» в модели), 4 услуги (мастер-класс, тир —
это `service`, не лот) и 3 антуража. `reslot` теперь берёт подсказку из
`specs.path` (две последние крошки) + категории — как импорт. «Мечи из Печи»
(`importers/orgsbiz.py`, один код на `*.orgs.biz`): 13 позиций фабричного ПУ,
100 % со слотом, описания добираются со страниц товаров; у всех «30 Шор», у
двух — длина/клинок/вес. Досеяно 100+ RU-алиасов (гроссмессер, федершверт,
полэкс, ронкона, лорика сегментата, маника, полуножны, техногенное: бита,
вилы, кирка, сковорода → `weapon_other`; зелья, палочки, кандалы → `props`).
Словарь **v9: 1 880 алиасов**.

**Фид — квота вместо даты.** Сортировка по `posted_at` выталкивала 4 500
RU-карточек без даты публикации (donjon, wargearshop) за потолок 5 000 — в
фиде было 13 RU-строк из 4 500. Теперь `_select_lots`: все объявления,
затем витрины по квоте `RU_SHARE=0.6` остатка, внутри группы источники
по кругу. Фид 18:20: 5 000 строк — 174 объявления, RU 2 908 (donjon 1 283
целиком, wargearshop 1 612, «Мечи из Печи» 13), DE 804, DK 644, CA 644;
со слотом 97 %. Chromium: 5 000 строк, 0 ошибок JS, снимок
`docs/site-live-2026-09-15-ru.png`.

Три числа (18:20, `hmb status`): **лотов 13 718 · со слотом 13 097 (95.5 %) ·
объявлений 174** (161 DE + 13 RU б/у); 68 DE-объявлений в inbox ждут
`classify`. Тестов 31.

**Раскладка страницы (замечание владельца, 18:40).** Противоречие: фильтры
слева, а выбор архетипа, кукла и таблица дефицита (кликабельная, ставит
фильтр по слоту) — справа. Перенесено: **левая панель — всё, что фильтрует**
(архетип · регламент, кукла с подсветкой выбранного слота, «слот закрыт /
конфликтов», подписки, цена, фасеты, дефицит рынка); **правая — сводка по
выбранному предмету** (название, цена и в валюте лота, медиана/перцентиль по
слоту, обмеры и посадка, период против архетипа, мастер, источник, кнопки
«оригинал ↗» — теперь настоящая ссылка на `sourceUrl` — и «подробнее» →
прежняя выдвижная панель). Клик по строке выбирает предмет (повторный —
снимает), Esc закрывает панель, не сбрасывая выбор; на узких экранах
прячется сводка, а не фильтры. «Возраст» у карточек без даты — «—», не
«nullд». Chromium: 0 ошибок, снимок `docs/site-rails-2026-09-15.png`.

**Картинки витрин (решение владельца, 18:50: «показывать по прямой ссылке на
их CDN»).** Первая правка схемы после `create_all`: `lots.image_url` —
добавлена через реестр `db._ADDED_COLUMNS` (`ALTER TABLE` идемпотентно;
Alembic по-прежнему не заведён, реестр — временная замена). Пишут четыре
импортёра витрин (Shopify — `images[0].src`; Донжон — миниатюра 169×169,
заглушку `default.jpg` не берём; WarGear — CDN Storeland; «Мечи из Печи» —
подписанные URL VK, могут протухнуть). У б/у (`kind=ad`) поле пусто по
правилу `DATA_SOURCES` §5. Страница: миниатюра 36 px в строке (`loading=lazy`,
`referrerpolicy=no-referrer`, при ошибке прячется), картинка сверху сводки и
в выдвижной панели с подписью «с сайта магазина». Chromium на «Мечах из
Печи»: 13/13 миниатюр загрузились, снимок `docs/site-images-2026-09-15.png`.
Спецификации предмета в сводке подписаны по-русски (общая длина, клинок,
масса, жёсткость Шор A).

**marktplaats.nl — вторая доска (19:10).** Next.js: список лежит в
`__NEXT_DATA__` (itemId, цена в центах + тип FIXED/FAST_BID, город, дата
«Vandaag/Gisteren/2 sep 26», фото, структурное состояние Nieuw / Zo goed als
nieuw / Gebruikt) — разбор JSON, не разметки. 8 запросов внутри категории
«Kostuums, Theaterbenodigdheden en LARP», по 3 страницы, пауза 3 с, блока не
было (24 запроса). Первый прогон: **316 просмотрено, 217 новых**.
Категория смешана с косплеем и вообще чем угодно (фурсьюты, парики, детская
коляска «Babyzen yoyo»): первый `classify` дал 43 без слота из 217. Введён
**гейт по слоту** для досок со смешанной категорией (`sources.meta.gate =
"slot"`): без слота → `skipped/off_topic`, не лот. После NL-композитов
(`Ridderkostuum`, `larpwapens` — те же головы, что для DE) и NL-множественных
(`zwaarden`, `schilden`, `wapens`, `helmen`…) — **190 лотов, 0 без слота,
27 off_topic** (все 27 — не снаряжение). Структурное состояние площадки
надёжнее регекса: `classify` берёт `extraction_meta.condition`, если импортёр
положил; у marktplaats `used` 124 · `new` 69 · `unknown` 24, цена есть у 80
(остальное «bieden»). Объявлений в базе **431** (228 DE · 190 NL · 13 RU).
Словарь 1 919 алиасов, 33 теста.

**Git.** Владелец дал адрес `github.com/dmitry00-00/LARP`; SSH-ключ машины уже
авторизован как `dmitry00-00` (тот же, что у recruit) — `origin` добавлен,
`main` (`9f03e65`) запушен 19:00. Pages включает владелец (Source: GitHub
Actions). Работа после первого коммита копится незакоммиченной по его слову.

**Сайт опубликован (19:25).** Владелец включил Pages (Source: GitHub Actions);
пуш `600f435` + `site/README` → workflow отработал, `https://dmitry00-00.github.io/LARP/`
отвечает 200, заголовок «реестр снаряжения для ЛАРП», фид 4.1 МБ, страница
рендерит 5 000 строк без ошибок JS. Заглушка `seed.js` в артефакте — чтобы не
было 404 в консоли. Миниатюры: `loading="lazy"` внутри прокручиваемого `#grid`
в Chromium не запускал ни одного запроса — заменён на свой
`IntersectionObserver` с корнем `#grid` (WarGear: 16 видимых → 16 загружены,
после прокрутки 40/40). **Странность источника:** `donjon.ru` отвечает
Chromium за 27–30 с (и картинки, и главная), Firefox/WebKit/curl — за 0.1–0.2 с;
не заголовки и не PQ-ключ TLS — что-то на их стороне в паре с Chromium. У
пользователей Chrome миниатюры Донжона будут висеть; остальные пять витрин —
мгновенно. Не лечим, записано как странность источника.

**Ещё две RU-витрины и услуги (19:05–19:30).** «Секира» (`importers/insales.py`
— InSales отдаёт JSON без ключа, один код на все InSales-магазины): 86
коллекций из меню, 91 запрос, **1 781 карточка, 95.5 % со слотом**, у 1 258 —
масса из веса варианта; непокрытое — открытки, блокноты, ткани, обложки на
паспорт (не снаряжение). «Кузница» bsmith.ru (`importers/bsmith.py`, Drupal):
`Crawl-delay: 10` в robots — соблюдаем, 163 раздела ≈ 30 мин, обход идёт;
пробные 3 раздела 125/125 со слотом; все лоты `made_to_order` (мастерская),
сроки по типам — в общем тексте сайта, не в карточках. Досеяно
раннесредневековое (худ, хвостовик, концевики, обоймица, копоушка) — 1 942
алиаса.

**Услуги — третья сторона рынка** (`hmb/services.py`): вид по разделу
(аренда · ремонт · подгонка · на заказ · мероприятие · оценка), по названию —
только явное («в аренду», «услуга», «мастер-класс»). Первая версия судила по
одному слову в названии и дала 3 ложных из 8 («Repair Glue» — клей,
«Kontaktlinsen-Event-Kit» — линзы, «(под заказ)» — товар со сроком, теперь
`specs.made_to_order`). Итог: 4 услуги (WarGear: мастер-класс, тир, ярмарка,
детский городок), `direction=service`, в фиде `kind` = вид услуги, вкладка
«Услуги» на странице их показывает; услуги и объявления попадают в фид
целиком, вне квоты витрин. 34 теста.

`reslot` дважды считал витрины беднее импорта (у Shopify — без тегов, у
InSales — без категории VK): оба поля теперь в `specs` и в пересчёте —
правило «диагностика повторяет прод-логику и отстаёт» (AGENT_RULES §13)
сработало дважды за вечер.

Три числа (19:30, `hmb status`): **лотов 16 523 · со слотом 15 766 (95.4 %) ·
объявлений 431**; витрин 9 (Кузница дочитывается).

**DE-витрины и RU-доска (19:30–19:45).** Mytholon (`importers/shopware.py`,
Shopware 6): robots `Disallow: /*?` запрещает query-строки, а пагинация у них
`?p=N` — берём только первую страницу каждой из 196 категорий, покрытие
каталога частичное и записано в `meta` («объявлено по категориям» против
«взято»). Andracor (`importers/oxid.py`, OXID; `?pgNr=N` не запрещён): 89
категорий, **1 893 карточки, 85.5 % → 96.2 %** после посева грима/линз/
париков/бластеров (`prosthetics`, `props`). allrpg.info «Склад»
(`importers/allrpg.py`): единственная профильная RU-доска с открытой выдачей —
13 позиций, аренда и продажа, без дат; позиция только с ценой аренды →
`direction=service`, вид `rent` (3 робота-костюма, рации). Всего источников
объявлений — **четыре** (kleinanzeigen · marktplaats · Донжон-Барахолка ·
allrpg), ROADMAP Г1 требует ≥5.

**Первый замер «сколько лотов в неделю даёт рынок»** (ROADMAP Г1, по
`posted_at` объявлений на 19:40): kleinanzeigen — 68 за текущую неделю
(неполную), **34** за прошлую, 15 · 12 · 7 · 8 за w-2…w-5; marktplaats —
73 · **17** · 17 · 10 · 19 · 7. Спад по неделям — не только темп публикации,
но и выживаемость объявления (проданное исчезает) и глубина обхода (3
страницы). Честная нижняя граница: **DE ≈ 30–35/нед., NL ≈ 15–20/нед.** —
выше порога разворота «< 20/нед.» для DE/NL-контура; по RU датированного
потока нет (VK/TG — за стеной владельца).

Не подключено сознательно: kufar.by — HTML отвечает 403 (antibot), открыт
только внутренний search-API; по правилу «не обходить 403» — не берём.

**Итог вечера (20:00, `hmb status`): лотов 22 138 · со слотом 21 156
(95.6 %) · объявлений 444 · услуг 8 · витрин 11 · источников 36 · словарь
2 104 алиаса · 36 тестов.** Кузница дочитана: 164 раздела, 165 запросов по
10 с, **2 462 карточки, 99.4 %** после посева (шашки, нагайки, кованые вилки).
Фид 5 000: RU 2 761 · DE 1 320 · DK 365 · CA 364 · NL 190. Витрины по
покрытию словаря: Кузница 99.4 · Донжон 99.9 · WarGear 99.3 · Andracor 97.1 ·
Calimacil 96.7 · Секира 95.5 · Dein LARP Shop 91.6 · Mytholon 90.3 (миниатюры,
мёд, литература — не снаряжение) · Epic Armoury 89.4 (имена собственные без
типа) · «Мечи из Печи» 100.

### Что осталось после §7

* kleinanzeigen: расписание — раз в 15 мин, прогон сам останавливается на 403
  (4–7 запросов), курсор ротации есть; нужен launchd/cron у владельца;
* страница переименована («реестр снаряжения для ЛАРП», 17:40); бренд
  «HMB-Market» в шапке — отдельное решение владельца (записка о нише);
* GitHub Pages — репозиторий заведён (`9f03e65`), ждёт адреса удалённого;
* услуги (мастер-классы, тир, аренда) сейчас либо без слота, либо лот: нужна
  `direction=service` на импорте по разделу;
* конфликт на странице — по годам архетипа (ИСБ); под ЛАРП архетип = система
  правил, конфликт = предел `ruleset_limits` против `specs` — вопрос владельцу;
* остальное из §4 без изменений (OLX из KZ/UA, VK/TG-чаты, Alembic).
